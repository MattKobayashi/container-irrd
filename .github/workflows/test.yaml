---
jobs:
  image:
    name: Image
    permissions:
      contents: read
    runs-on: ubuntu-24.04
    steps:
      # Harden the runner (Audit all outbound calls)
      # https://github.com/step-security/harden-runner
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@95d9a5deda9de15063e7595e9719c11c38c90ae2 # v2.13.2
        with:
          egress-policy: audit
      # Checkout repository
      # https://github.com/actions/checkout
      - name: Checkout repository
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
      # Build and start image
      - name: Build and start image
        run: |
          docker compose up --detach
        working-directory: tests
      # Readiness check
      - name: Readiness check
        run: |
          timeout=300
          interval=10
          end=$((SECONDS+timeout))

          echo "Waiting for IRRd to be ready at http://127.0.0.1:8080/v1/status ..."
          until curl -fsS http://127.0.0.1:8080/v1/status | grep -q "Listening on"; do
            if (( SECONDS >= end )); then
              echo "IRRd not ready within ${timeout}s" >&2
              exit 1
            fi
            echo "IRRd not ready yet; retrying in ${interval}s..."
            sleep ${interval}
          done
          echo "IRRd is ready."

          echo "Waiting for IRR Explorer to be ready at http://127.0.0.1:8000/ ..."
          until curl -fsS http://127.0.0.1:8000/ | grep -q "<title>IRR explorer</title>"; do
            if (( SECONDS >= end )); then
              echo "IRR Explorer not ready within ${timeout}s" >&2
              exit 1
            fi
            echo "IRR Explorer not ready yet; retrying in ${interval}s..."
            sleep ${interval}
          done
          echo "IRR Explorer is ready."
      # Grab the container logs
      - if: ${{ !cancelled() }}
        name: Grab container logs
        run: |
          docker compose logs irrd
        working-directory: tests
      # Stop and remove containers
      - if: ${{ !cancelled() }}
        name: Stop and remove container
        run: |
          docker compose down
        working-directory: tests
  shell:
    name: Shell
    permissions:
      contents: read
      security-events: write
    runs-on: ubuntu-24.04
    steps:
      # Harden the runner (Audit all outbound calls)
      # https://github.com/step-security/harden-runner
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@95d9a5deda9de15063e7595e9719c11c38c90ae2 # v2.13.2
        with:
          egress-policy: audit
      # Checkout repository
      # https://github.com/actions/checkout
      - name: Checkout repository
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
      # Run ShellCheck
      # https://www.shellcheck.net/wiki/GitHub-Actions
      - name: Run ShellCheck
        run: find . -type f -name "*.sh" -exec shellcheck {} +
name: Test
on:
  pull_request:
    branches:
      - main
  workflow_dispatch:
permissions:
  contents: read
